# Databricks App runtime + in-process agent.

flask>=3.0.3
gunicorn>=22.0.0          # prod WSGI server (Render / any container); dev uses flask run
psycopg2-binary>=2.9.9
databricks-sdk>=0.40.0
databricks-sql-connector>=3.4.0
requests>=2.32.3
python-dotenv>=1.0.1

# In-process LangGraph agent. databricks-langchain is PINNED to the 0.4 line:
# >=0.5 pulls databricks-connect, and 0.16.x requires langchain>=1.0 + databricks-mcp
# + databricks-openai, which explodes pip's resolver. 0.4.0 needs only the
# langchain 0.3 line, so everything below is pinned to match.
databricks-langchain==0.4.0
langchain>=0.3.20,<0.4
langchain-core>=0.3.33,<0.4
langgraph>=0.3,<0.4
mlflow>=2.20.1,<4          # hard dep of databricks-langchain (needs mlflow.deployments).
                          # The Dockerfile swaps this for mlflow-skinny + drops pyarrow
                          # to fit a 512MB instance; tracing (mlflow.langchain) is optional.

# Filing parsing — used by the Spark notebooks; lib/edgar_parse imports these
# lazily so the app process doesn't load them.
beautifulsoup4>=4.12.0
lxml>=5.2.0
